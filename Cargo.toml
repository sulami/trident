[package]
name = "trident"
version = "0.2.0"
edition = "2021"
authors = [ "Robin Schroer" ]
description = "Runs programs in parallel"
license = "MIT"

[dependencies]
anyhow = "1.0.75"
clap = { version = "4", features = ["derive"] }
rayon = "1.8.0"
